{
  "name": "justone-catalog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone DLHD-filtered IPTV catalogue, account allocator and MPEG-TS stream proxy for Jellyfin.",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "check": "for f in src/*.js; do node --check \"$f\" || exit 1; done && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
